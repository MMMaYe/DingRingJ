# 多 Agent 群聊讨论系统 - DDD 事件风暴成果

> 本文档是事件风暴的完整产出，作为后续战术设计和编码的依据。

## 一、产品定位

### 1.1 解决的问题

失业后一个人学习，缺少技术讨论的对撞感。单一 Agent 问答枯燥，希望还原"和同事在群聊里聊技术"的氛围。

### 1.2 核心价值

通过多个 AI Agent（DeepSeek/Kimi/Claude 等）扮演不同技术角色，在群聊中围绕主题进行辩论式讨论，最终由专家 Agent 汇总结论。讨论结论可沉淀为知识卡片，形成"讨论 → 沉淀 → 复习 → 再讨论"的学习闭环。

### 1.3 开源目标

作为个人第一个开源作品发布到 GitHub，增加面试机会。

---

## 二、限界上下文划分

### 2.1 上下文地图

```
┌─────────────────────────────┐  ┌─────────────────────────────┐
│     核心域 1：群聊讨论        │  │     核心域 2：知识卡片        │
│                             │  │                             │
│  Chatroom 聚合              │  │  Topic 聚合根               │
│  Discussion 聚合            │──→│  Question 实体              │
│  (消息/轮次/私聊/引用)       │  │  Card 实体                  │
│                             │  │  ReviewSession 实体          │
└─────────────────────────────┘  └─────────────────────────────┘
            │  │  │
            ▼  ▼  ▼
┌─────────────────────────────┐  ┌─────────────────────────────┐
│        支撑域                │  │        通用域                │
│                             │  │                             │
│  调度域    知识库域          │  │  结论生成域   消耗仪表盘     │
│  Token预测域  LLM适配域      │  │  用户域                     │
└─────────────────────────────┘  └─────────────────────────────┘
```

### 2.2 限界上下文清单

| 限界上下文 | 子域类型 | 核心职责 |
|---|---|---|
| **群聊域** | 核心域 | 群聊创建/管理，Agent 角色配置（花名/头像/性格/API KEY/LLM 供应商），讨论列表管理（同一时间只有一个 ACTIVE 讨论） |
| **讨论域** | 核心域 | 一次讨论的完整生命周期：消息（主讨论/私聊/引用/@）、轮次控制、状态流转、滑动窗口上下文（可配置，默认 200） |
| **知识卡片区** | 核心域 | Topic 自动创建（取讨论主题名），一个 Topic 下多个 Question，一个 Question 对应一张 Card，自由回忆复习，顺序可选，不记录复习结果 |
| **调度域** | 支撑域 | 自由式发言选择（6 因子评分）、引用加权、@加权、故障接力决策、私聊发起决策 |
| **知识库域** | 支撑域 | 文档管理、向量化、检索、RAG，一个群聊可配多个知识库，调度域决策是否引用 |
| **Token 预测域** | 支撑域 | 按主题预估消耗、自建额度计算模块（处理不返回 token 数的 API）、按讨论汇总统计 |
| **LLM 适配域** | 支撑域 | 多供应商适配（DeepSeek/Kimi/Claude 等）、API KEY 管理 |
| **结论生成域** | 通用域 | 专家 Prompt 模板、结论生成（可多次生成，不锁定讨论）、HTML 导出（仅结论，异步通知） |
| **消耗仪表盘** | 通用域 | 纯读查询展示，无业务逻辑 |
| **用户域** | 通用域 | 用户认证、个人配置 |

---

## 三、角色 - 命令 - 事件映射

> 事件风暴三要素：**角色（Actor，橙色）→ 命令（Command，蓝色）→ 事件（Event，黄色）**。
> 链路关系：Actor 发出 Command，Command 执行后产生 Event。

### 3.0 角色清单

| 角色 | 类型 | 说明 |
|---|---|---|
| 用户 | 人 | 系统使用者，发起配置/讨论/复习等操作 |
| 调度域 | 内部子系统 | 选择发言者、发起私聊、故障接力决策 |
| Agent | AI | 群聊中扮演技术角色的 AI，发公开回复或私聊 |
| 专家 Agent | AI | 专门汇总结论的特定 Agent |
| 系统 | 平台 | 自动触发的动作（Token 预测、卡片生成、失败标记等） |

### 3.1 准备阶段

| Actor | Command | Event | 说明 |
|---|---|---|---|
| 用户 | 创建群聊 | ChatroomCreated | |
| 用户 | 配置 Agent 角色 | AgentRolesConfigured | 花名/头像/性格/API KEY/LLM 供应商，支持预设模板+自定义 |
| 用户 | 关联知识库 | KnowledgeBaseAttached | 可多个 |
| 用户 | 发起讨论 | DiscussionCreated | 同一群同时只一个 ACTIVE |
| 系统 | 预测 Token 消耗 | TokenConsumptionPredicted | 按主题预估 |
| 用户 | 开始讨论 | DiscussionStarted | |

### 3.2 讨论阶段（循环）

| Actor | Command | Event | 说明 |
|---|---|---|---|
| 用户 | 发送消息 | UserMessaged | 可插话 |
| 用户 | 引用回复 | MessageReplied | 被引用者优先级提升，不打断当前 Agent |
| 用户 | @提及 Agent | AgentMentioned | 被@者优先级提升 |
| 调度域 | 触发 Agent 轮次 | AgentTurnTriggered | 6 因子评分 |
| 调度域 | 发起私聊 | PrivateChatInitiated | Agent 自主发起 |
| 调度域 | 决策接力 | AgentFailedOver | 失败后接力，失败 Agent 下轮仍尝试 |
| Agent | 回复消息 | AgentResponded | 滑动窗口 200 条作上下文，窗口可配置 |
| Agent | 发送私聊消息 | PrivateChatMessageSent | 对用户可见，不进滑动窗口 |
| 系统 | 标记 Agent 失败 | AgentFailed | LLM 返回报错 |
| 系统 | 标记全部失败 | AllAgentsFailed | 通知用户 |

### 3.3 收尾阶段

| Actor | Command | Event | 说明 |
|---|---|---|---|
| 用户/系统 | 结束讨论 | DiscussionEnded | @专家 / 轮次上限 / 全部失败 |
| 用户 | 请求生成结论 | SummaryRequested | 全量消息 + 专家 Prompt |
| 专家 Agent | 生成结论 | SummaryGenerated | 可多次生成，不锁定讨论 |
| 用户 | 导出讨论 | DiscussionExported | 仅结论，HTML 格式，异步通知 |
| 用户 | 重新打开讨论 | DiscussionReopened | |

### 3.4 知识卡片阶段

| Actor | Command | Event | 说明 |
|---|---|---|---|
| 系统 | 生成知识卡片 | CardsGenerated | Topic 自动创建，取讨论主题名 |
| 用户 | 开始复习 | ReviewStarted | 选 Topic，顺序可选 |
| 系统 | 展示问题 | CardShown | |
| 用户 | 显示答案 | AnswerRevealed | 自行判断是否记住 |
| 用户 | 结束复习 | ReviewCompleted | 不记录复习结果 |

### 3.5 后台持续

| Actor | Command | Event | 说明 |
|---|---|---|---|
| 系统 | 记录 Token 消耗 | TokenConsumptionRecorded | 每条消息 |
| 用户 | 配置滑动窗口 | SlidingWindowConfigured | 默认 200 |

---

## 四、关键业务规则

### 4.1 讨论流程

1. **同一群聊同一时间只能有一个 ACTIVE 讨论**，必须结束当前讨论才能开始新的
2. **Agent 发言是自动循环**：Agent 回复后自动触发下一轮，用户被动看
3. **用户可随时插话**，可发普通消息、引用回复、@特定 Agent
4. **滑动窗口默认 200 条**，可配置，包含所有主讨论消息（含其他 Agent 发言），**不含私聊消息**
5. **私聊对用户可见但不进滑动窗口**，私聊上下文是该私聊会话的全部消息
6. **引用回复**：被引用的 Agent 优先级提升，但不打断当前正在回复的 Agent
7. **@提及**：被@的 Agent 优先级提升，同样不打断当前回复

### 4.2 Agent 调度

**自由式发言评分模型（6 因子）**：

| 因子 | 说明 |
|---|---|
| 轮转权重 | 距离上次发言轮次越久，分数越高 |
| 话题相关度 | 上一轮内容与该 Agent 角色领域的匹配度 |
| 冲突检测 | 上一个 Agent 观点与该 Agent 角色立场相反，分数提高 |
| 随机因子 | 小幅随机扰动，避免完全可预测 |
| 引用加权 | 被引用的 Agent 分数提升 |
| @加权 | 被@的 Agent 分数提升 |

### 4.3 故障处理

1. **失败标准**：LLM 返回报错（API 超时不算失败？需确认）
2. **接力决策**：由调度域决策接力对象
3. **失败 Agent 下轮仍尝试**：不永久排除
4. **全部失败**：通知用户

### 4.4 专家结论

1. **结束条件**：@专家 / 轮次上限 / 全部失败
2. **结论生成后不锁定讨论**，可继续讨论后再次请求生成新结论
3. **导出仅导出结论**，HTML 格式，异步生成完通知

### 4.5 知识卡片

1. **Topic 自动创建**，取讨论的主题名
2. **一个 Topic 下多个 Question，一个 Question 对应一张 Card**
3. **卡片不跨 Topic**
4. **复习形式**：自由回忆，顺序可选
5. **不记录复习结果**
6. **答案不更新**，仅作参考
7. **知识卡片内容不进入知识库**

### 4.6 Token 统计

1. **按讨论统计**，需要有一个预测主题消耗多少 Token 的域
2. **自建额度计算模块**，根据指标计算（处理不返回 token 数的 API）
3. **无预算限制**，超了不影响（API Key 额度消耗完了就是消耗完了）

---

## 五、Agent 配置项

| 配置项 | 说明 |
|---|---|
| API KEY | 每个 Agent 单独配置一个 |
| 花名 | 角色名称，在群聊中显示 |
| 头像 | 预设的，非上传 |
| 性格描述 | 轻量描述（非完整 System Prompt） |
| LLM 供应商 | DeepSeek / Kimi / Claude 等 |

---

## 六、技术栈

- Java + Spring Boot + Spring AI
- 单体应用，个人/小团队（1-5 人）
- 后续可能拆微服务

---

## 七、待确认问题

> 以下问题在事件风暴中未完全明确，留待战术设计阶段确认。

1. **API 超时算不算失败**：目前只说"LLM 返回报错"才算失败，超时的处理方式未明确
2. **Agent 表情/反应**：是否支持 Agent 对消息发 👍/❓（未确认）
3. **消息置顶/标记**：是否支持用户标记关键观点（未确认）
4. **讨论模板保存复用**：是否支持保存"群名+角色+轮次上限"一套配置（未确认）
5. **公开分享链接**：是否支持生成只读查看链接（未确认）
6. **讨论分支**：是否支持从一个点岔开单独讨论（未确认）
7. **Agent 情绪状态**：是否影响发言风格（未确认）
8. **讨论评分**：专家结论是否对讨论质量打分（未确认）

---

## 八、学习闭环（产品核心价值）

```
讨论（多 Agent 辩论）
  → 沉淀（结论生成 → 知识卡片）
    → 复习（自由回忆/顺序可选）
      → 再讨论（基于复习发现的新问题）
        → ...循环
```

**核心理念**：通过多 Agent 讨论生成知识 → 沉淀为可复习卡片 → 复习发现新问题 → 再次发起讨论。这是产品区别于普通 AI 群聊工具的核心价值。
