# 提示词最佳实践

> 基于 [Amp (Sourcegraph)](https://github.com/x1xhlol/system-prompts-and-models-of-ai-tools/tree/main/Amp) 的 Claude Sonnet 4 和 GPT-5 两套系统提示词深度分析，提炼工程化提示词设计方法论。

---

## 概述

Amp 是 Sourcegraph 出品的 AI 编程助手。其系统提示词在短短数月内经历了从 Claude Sonnet 4 到 GPT-5 的显著迭代——后者更精简、更结构化、更强调并行执行和验证闭环。这两份提示词共同展示了**面向 AI Agent 的提示词工程**从"告诉模型做什么"到"定义模型如何思考"的范式转变。

本文档从中提炼出 **12 条核心原则**，按"定义-约束-执行-验证-沟通"五个维度组织，每条原则均包含正反案例和适用边界。

---

## 核心原则速查表

| 维度 | # | 原则 | 一句话概括 |
|------|---|------|-----------|
| 定义 | 1 | **先定义角色，再描述任务** | 开头一句话明确你是谁、能做什么、不能做什么 |
| 定义 | 2 | **用结构化标签组织内容** | 用 `# 标题`、`## 子标题` 和 `<xml>` 标签为模型建立心智地图 |
| 约束 | 3 | **用 Guardrails 设硬边界** | 在任务开始前定义"不许做"清单，比"应该做"更有效 |
| 约束 | 4 | **用分隔符隔离动态内容** | 将用户输入、环境变量用 `<tags>` 包裹，避免与指令混淆 |
| 执行 | 5 | **默认并行，显式串行** | 告诉模型何时可以并行，何时必须串行 |
| 执行 | 6 | **给出工具选择策略，而非工具清单** | 教模型"什么时候用什么工具"，而不是列出所有工具签名 |
| 执行 | 7 | **用示例驱动行为，而非规则描述** | 一个 `<example>` 胜过三段抽象说明 |
| 执行 | 8 | **定义三层决策路由** | 区分"问专家"、"查资料"和"直接干"三种子任务类型 |
| 验证 | 9 | **定义验证门禁顺序** | 指定 Typecheck → Lint → Tests → Build 的固化验证链 |
| 验证 | 10 | **要求状态可见性** | 强制模型使用 Todo 工具展示进度，避免黑盒执行 |
| 沟通 | 11 | **规定输出格式规范** | 精确到标点符号、链接格式、代码块语言的输出要求 |
| 沟通 | 12 | **强制简洁，杜绝废话** | 用"4 行以内"等硬指标约束模型输出长度 |

---

## 分场景详解

---

### 原则一：先定义角色，再描述任务

**原则**：提示词的第一段必须完成"身份声明"，让模型在进入具体任务前建立行为框架。

**背景**：两份 Amp 提示词都以完全相同的句子开头——"You are Amp, a powerful AI coding agent built by Sourcegraph. You help the user with software engineering tasks."——然后才进入具体指令。这不仅是品牌陈述，更是**认知锚点**：它告诉模型"你是工具使用者，不是创造者；你是助手，不是主导者"。

**案例**：

✅ **推荐做法**（Amp Claude Sonnet 4 的开头）：

```
You are Amp, a powerful AI coding agent built by Sourcegraph. You help
the user with software engineering tasks. Use the instructions below
and the tools available to you to help the user.
```

在身份声明之后立即进入 Agency 章节，详细说明"主动性和约束的平衡"——这是一层更精细的角色定义。

❌ **常见反模式**：

```
You are an AI assistant. You help with coding tasks.
```

过于模糊，没有说明"你属于哪个系统"、"你和用户是什么关系"、"你的主动性边界在哪里"。模型可能会过度主动（用户问方案时直接改代码）或过度被动（需要反复催促才行动）。

**Amp 中的进阶用法**：GPT-5 版本在角色定义基础上增加了"**做事风格**"定义：

```
- Do the task end to end. Don't hand back half-baked work.
- Balance initiative with restraint: if the user asks for a plan, give a plan; don't edit files.
- Do not add explanations unless asked. After edits, stop.
```

**适用边界**：
- 适用于所有需要模型扮演特定角色的场景（编程助手、客服、数据分析师等）
- 对于纯知识问答场景（如"什么是量子计算"），角色定义可以更简洁
- 对于需要多角色切换的复杂场景，建议为每个角色单独定义，而非混合在一个提示词中

---

### 原则二：用结构化标签组织内容

**原则**：使用 Markdown 标题层级和 XML 标签构建提示词的"信息架构"，让模型能快速定位和引用不同部分。

**背景**：Amp 的两份提示词都大量使用 `#` 标题（Agency、Oracle、Task Management、Conventions & Rules...）和 `<example>`、`<user>`、`<response>` 等 XML 标签。这种结构让模型在长上下文环境中能快速定位相关部分。

**案例**：

✅ **推荐做法**（Amp 的层次结构）：

```
# Agency
## 具体行为准则
## 推荐工作流程

# Oracle
## 使用时机
## 示例

# Task Management
## 工具说明
## 示例
```

❌ **常见反模式**：

```
You are an AI assistant. You should do the following:
1. Use tools to help users
2. Plan tasks before doing them
3. Be concise in responses
...
```

使用平铺的编号列表，没有层次结构。当提示词超过 50 行时，模型难以区分"这是行为准则还是工具使用说明"。

**Amp 中的进阶用法**：GPT-5 版本使用了更细粒度的标签结构：

```
# Guardrails (Read this before doing anything)
- **Simple-first**: ...
- **Reuse-first**: ...
- **No surprise edits**: ...
- **No new deps**: ...
```

用括号标注优先级："(Read this before doing anything)" 告诉模型这段内容的重要性级别。

**适用边界**：
- 标题层级不要超过 4 级（`#` → `####`），否则会降低可读性
- XML 标签名要有语义（`<example>` 好于 `<tag1>`）
- Markdown 标题和 XML 标签应各司其职：标题用于"章节组织"，标签用于"内容类型标记"

---

### 原则三：用 Guardrails 设硬边界

**原则**：在任务描述之前，先明确列出"不能做的事"，用否定式约束为模型划定安全边界。

**背景**：GPT-5 版本在开篇新增了 `# Guardrails` 章节，放在所有行为描述之前。这是 Claude 版本没有的改进。Claude 版本将约束分散在 `# Conventions & Rules` 中，GPT-5 将其前置并命名为"Guardrails"——暗示这些是**不可协商的硬约束**。

**案例**：

✅ **推荐做法**（GPT-5 的 Guardrails）：

```
# Guardrails (Read this before doing anything)
- **Simple-first**: prefer the smallest, local fix over a cross-file "architecture change".
- **Reuse-first**: search for existing patterns; mirror naming, error handling, I/O, typing, tests.
- **No surprise edits**: if changes affect >3 files or multiple subsystems, show a short plan first.
- **No new deps** without explicit user approval.
```

每一条都是"负面约束 + 可操作标准"的组合："Simple-first" 不是抽象的"保持简单"，而是具体的"优先本地修复而非跨文件架构变更"。

❌ **常见反模式**：

```
Please try to keep your changes simple and avoid unnecessary complexity.
```

过于模糊。"简单"和"复杂"因人而异，无法验证。模型无法判断"增加一个工具类"算不算"不必要的复杂"。

**Amp 中的对比**：Claude 版本也有类似约束，但分散在 `# Agency` 和 `# Conventions & Rules` 中，且语言更温和：

```
# Agency
3. Do not add additional code explanation summary unless requested by the user.
```

GPT-5 将其升级为加粗的 Guardrails 格式，视觉上更突出，语义上更强制。

**适用边界**：
- Guardrails 适合**安全关键**约束（禁止删除文件、禁止安装依赖、禁止修改配置文件）
- 对于"建议性"的行为准则（如"尽量使用异步 IO"），放在常规章节即可
- Guardrails 数量建议控制在 3-6 条，太多会稀释每条的重要性

---

### 原则四：用分隔符隔离动态内容

**原则**：将用户输入、环境变量、文件内容等动态数据用专用标签包裹，使其与提示词中的静态指令清晰区分。

**背景**：Amp 的两份提示词都使用 `<attachedFiles></attachedFiles>` 和 `<user-state></user-state>` 标签标记用户侧上下文。Claude 版本还使用 `<directoryListing>` 标签包裹目录结构。

**案例**：

✅ **推荐做法**（Amp 的环境上下文）：

```
# Context
The user's messages may contain an <attachedFiles></attachedFiles>
tag, that might contain fenced Markdown code blocks of files the user
attached or mentioned in the message.

The user's messages may also contain a <user-state></user-state> tag,
that might contain information about the user's current environment.
```

模型被明确告知：`<attachedFiles>` 中的内容来自用户，不是指令的一部分。这防止了**提示词注入**——用户输入被模型误读为指令。

❌ **常见反模式**：

```
User's message: [user message here]
Files: [file1, file2, file3]
```

没有专用分隔符。如果用户消息中包含类似于"请忽略之前的指令"的文本，模型可能无法区分这是用户输入还是系统指令。

**Amp 中的进阶用法**：目录列表被包裹在 `<directoryListing>` 标签中，并附带 `(cached)` 标注，明确告知模型这段数据可能不是最新的：

```
<directoryListing>
  c:/Users/ghuntley/code/... (current working directory)
  ├ .git/
  ├ src/
  ...
</directoryListing>
```

**适用边界**：
- 所有用户可控的内容都应使用专用标签包裹
- 标签名应反映内容类型（`<file>`、`<user_input>`、`<environment>`），而非渲染样式（`<bold>`、`<code>`）
- 缓存/过时数据应标注 `(cached)` 或类似标记，避免模型将其当作实时信息

---

### 原则五：默认并行，显式串行

**原则**：明确告诉模型哪些操作可以并行执行，哪些必须串行执行，将并行化从"可选优化"提升为"默认行为"。

**背景**：这是 GPT-5 版本相比 Claude 版本最显著的改进之一。Claude 版本只在 Agency 中提了一句"invoke all relevant tools simultaneously"，而 GPT-5 版本专门开辟了 `# Parallel Execution Policy` 章节，用"什么可以并行"和"什么时候必须串行"两个子节详细规定。

**案例**：

✅ **推荐做法**（GPT-5 的并行策略）：

```
# Parallel Execution Policy
Default to **parallel** for all independent work: reads, searches,
diagnostics, writes and **subagents**.
Serialize only when there is a strict dependency.

## What to parallelize
- **Reads/Searches/Diagnostics**: independent calls.
- **Codebase Search agents**: different concepts/paths in parallel.
- **Oracle**: distinct concerns in parallel.
- **Task executors**: multiple tasks in parallel iff their write targets are disjoint.

## When to serialize
- **Plan → Code**: planning must finish before code edits that depend on it.
- **Write conflicts**: any edits that touch the **same file(s)** must be ordered.
- **Chained transforms**: step B requires artifacts from step A.
```

**Good parallel example** 和 **Bad parallel example** 提供了正反对比，让模型能精确判断"什么情况下并行是安全的"。

❌ **常见反模式**：

```
Try to use multiple tools at the same time when possible.
```

没有提供判断标准。模型可能在"修改同一个文件"时并行执行多个 Task，导致竞态条件和 diff 冲突。

**适用边界**：
- 并行策略的前提是底层工具支持无冲突的并行调用
- 写操作（文件修改、数据库写入）的并行需要额外防护（写锁、文件锁）
- 对于调试场景，串行执行（每一步验证结果）通常比并行更可靠

---

### 原则六：给出工具选择策略，而非工具清单

**原则**：不要只告诉模型"有哪些工具可用"，更要告诉模型"什么场景用什么工具"和"什么场景**不该**用什么工具"。

**背景**：Amp 的每个工具定义中都包含 `WHEN TO USE THIS TOOL` 和 `WHEN NOT TO USE THIS TOOL` 两个明确章节。这是提示词工程中一个容易被忽视但极其重要的设计模式。

**案例**：

✅ **推荐做法**（Amp 的 Grep 工具定义）：

```
WHEN TO USE THIS TOOL:
- When you need to find exact text matches like variable names, function calls, or specific strings
- When you know the precise pattern you're looking for (including regex patterns)

WHEN NOT TO USE THIS TOOL:
- For semantic or conceptual searches (e.g., "how does authentication work") - use codebase_search instead
- For finding code that implements a certain functionality without knowing the exact terms
```

"WHEN NOT TO USE" 部分不仅告诉模型"不该用"，还给出了替代方案——"use codebase_search instead"。

❌ **常见反模式**：

```
Available tools:
- Grep: Search for text patterns
- codebase_search: Semantic search
- Read: Read files
```

模型可能把 Grep 用于语义搜索（结果不佳），或把 codebase_search 用于精确查找（效率低）。工具越多，模型选错工具的概率越大。

**工具选择策略的分层设计**（GPT-5 版本的最佳实践）：

```
"1. need a senior engineer to think with me" → Oracle
"2. need to find code that matches a concept" → Codebase Search Agent
"3. know what to do, need large multi-step execution" → Task Tool
```

用"问题场景"映射到"工具"，而不是用"工具功能"映射到"用法"。

**适用边界**：
- 工具数量超过 3 个时，强烈建议使用 "WHEN TO USE / WHEN NOT TO USE" 模式
- 对于简单场景（如只有 1-2 个工具），显式策略的收益递减
- 策略应随模型的迭代而更新——不同模型对同一工具的使用能力不同

---

### 原则七：用示例驱动行为，而非规则描述

**原则**：用一个具体的 `<example>` 展示期望行为，胜过三段抽象的规则描述。

**背景**：Amp 的 Claude 版本大量使用 `<example>` 块。在 `# Agency` 章节中，有 10 个完整示例覆盖了从"查找命令"到"并行分发子任务"的各种场景。每个示例都包含 `<user>` 和 `<response>` 标签，展示完整的交互过程。

**案例**：

✅ **推荐做法**（Amp 的示例——展示"如何回应工具使用问题"）：

```
<example>
<user>Which command should I run to start the development build?</user>
<response>[uses list_directory tool to list files, then reads relevant files]
cargo run</response>
<user>Which command should I run to start release build?</user>
<response>cargo run --release</response>
</example>
```

这个示例同时教会了模型三件事：1) 先探索再回答；2) 回答要简洁（只输出命令）；3) 同类问题保持一致性。

❌ **常见反模式**：

```
When a user asks about how to run a command, you should first look at the 
project files to understand the build system, then provide the exact command. 
Keep your answer short and precise.
```

模型需要解析"first...then...keep..."的序列逻辑，容易出现遗漏或顺序错误。且没有展示"多短算短"。

**Amp 中的进阶用法**：示例还包含 `<rationale>` 标签，解释为什么这个示例展示了正确的行为：

```
<rationale>In the above example, the assistant completes all the 
tasks, including the 10 error fixes and running the build and fixing 
all errors.</rationale>
```

这帮助模型理解示例的"教学目的"，而不仅仅是模仿表面行为。

**适用边界**：
- 示例应覆盖**边界情况**（如"用户只问不要求执行"）和**典型情况**
- 示例数量不是越多越好——10 个精心设计的示例 > 30 个随意的示例
- 示例应保持一致性：如果示例 A 中模型用了一种回答风格，示例 B 中不应出现冲突的风格
- 对于简单规则（如"不要用 emoji"），规则描述比示例更高效

---

### 原则八：定义三层决策路由

**原则**：为模型建立"思考-搜索-执行"三层决策框架，让模型能根据任务复杂度自动选择处理策略。

**背景**：GPT-5 版本引入了三个子代理工具（Oracle、Codebase Search Agent、Task Tool），并明确给出了它们的选择策略：

```
"1. need a senior engineer to think with me" → Oracle
"2. need to find code that matches a concept" → Codebase Search Agent
"3. know what to do, need large multi-step execution" → Task Tool
```

这本质上是**决策路由**——将任务按照"不确定程度"分发到不同层级的处理单元。

**案例**：

✅ **推荐做法**（GPT-5 的子代理路由 + 推荐工作流）：

```
Workflow: Oracle (plan) → Codebase Search (validate scope) → Task Tool (execute)
```

先规划（Oracle），再验证范围（Codebase Search），最后执行（Task Tool）。这是一个完整的"思考-验证-执行"流水线。

❌ **常见反模式**：

```
You have the following sub-agents:
- Task: for complex tasks
- Oracle: for planning
- Codebase Search: for searching
```

模型可能不知道"复杂任务"和"规划"的区别，把所有任务都丢给 Task Tool。

**适用边界**：
- 三层路由适用于有多个子代理/工具的复杂 Agent 系统
- 对于单工具 Agent，可以用"先查后做"的两层路由替代
- Oracle（高推理成本）应谨慎使用，仅在需要深度分析时触发

---

### 原则九：定义验证门禁顺序

**原则**：要求模型在执行完成后按固定顺序运行验证步骤，形成"不可跳过"的质量门禁。

**背景**：GPT-5 版本新增了 `# Verification Gates` 章节，明确规定了验证顺序：Typecheck → Lint → Tests → Build。Claude 版本只在 Agency 中提了一句"after completing a task, you MUST run the get_diagnostics tool and any lint and typecheck commands"，没有指定顺序和范围。

**案例**：

✅ **推荐做法**（GPT-5 的验证门禁）：

```
# Verification Gates (must run)
Order: Typecheck → Lint → Tests → Build.
- Use commands from `AGENTS.md` or neighbors; if unknown, search the repo.
- Report evidence concisely in the final status (counts, pass/fail).
- If unrelated pre-existing failures block you, say so and scope your change.
```

"Unrelated pre-existing failures" 的说明很重要——它防止模型因为一个不相关的预存错误而陷入无限修复循环。

❌ **常见反模式**：

```
After making changes, make sure to run tests to verify your work.
"

没有指定顺序、范围、失败处理策略。模型可能只跑测试不跑类型检查，或在遇到预存错误时浪费时间修复无关问题。

**Amp 中的对比**：Claude 版本也要求运行验证，但语言更弱：

```
After completing a task, you MUST run the get_diagnostics tool and 
any lint and typecheck commands (e.g., pnpm run build, pnpm run check, 
cargo check, go build, etc.) that were provided to you.
```

GPT-5 版本将其拆分为独立章节，并增加了"失败处理"策略，是一个明显的进化。

**适用边界**：
- 验证门禁的顺序应遵循"越快越先"原则（Typecheck 通常比 Tests 快）
- 对于没有类型系统的语言（Python、Ruby），调整为 Lint → Tests → Build
- 验证命令应预先定义在 AGENTS.md 中，避免模型每次搜索

---

### 原则十：要求状态可见性

**原则**：强制模型使用 Todo 等进度追踪工具，让用户（和模型自己）能清晰看到当前进展和剩余工作。

**背景**：Amp 的两份提示词都极其强调 Todo 工具的使用，且有专门的 `# Task Management` 章节。Claude 版本甚至用"EXTREMELY helpful"和"that is unacceptable"这样的强烈措辞来强调其重要性。

**案例**：

✅ **推荐做法**（Claude 版本的任务管理要求）：

```
You have access to the todo_write and todo_read tools to help you
manage and plan tasks. Use these tools VERY frequently to ensure that
you are tracking your tasks and giving the user visibility into your
progress.
```

并给出了详细的渐进式 Todo 使用示例：先写大任务 → 运行构建发现 10 个错误 → 拆分为 10 个子任务 → 逐个完成。

❌ **常见反模式**：

```
Plan your tasks before executing them.
"

没有要求模型展示计划。模型可能在内部规划但用户看不到进度，也不知道模型在做什么。

**Amp 中的进阶用法**：GPT-5 版本进一步要求**即时标记**：

```
MARK todos as completed as soon as you are done with a task. Do not
batch up multiple tasks before marking them as completed.
```

这防止了模型"全部做完再一次性汇报"，提高了过程可见性。

**适用边界**：
- 状态可见性对复杂任务（>3 步）至关重要，对简单任务（1-2 步）可以省略
- 对于用户明确要求"直接做，不用汇报"的场景，可以允许跳过
- Todo 粒度应适中：太粗（只有"实现功能"一条）没有意义，太细（"按下回车键"）则过度琐碎

---

### 原则十一：规定输出格式规范

**原则**：精确到标点符号、链接格式、代码块语言的输出格式约束，消除模型的"审美自由度"。

**背景**：GPT-5 版本有专门的 `# Markdown Formatting Rules (strict)` 章节，从"bullet 只能用横线"到"代码块必须有语言标签"，从"不要用 emoji"到"文件链接必须用 file:// 协议"——事无巨细。这是 Claude 版本没有的完整章节。

**案例**：

✅ **推荐做法**（GPT-5 的输出格式规范）：

```
- Bullets: use hyphens `-` only.
- Numbered lists: only when steps are procedural; otherwise use `-`.
- Headings: `#`, `##` sections, `###` subsections; don't skip levels.
- Code fences: always add a language tag (`ts`, `tsx`, `js`, `json`, `bash`, `python`); no indentation.
- Inline code: wrap in backticks; escape as needed.
- Links: every file name you mention must be a `file://` link with exact line(s) when applicable.
- No emojis, minimal exclamation points, no decorative symbols.
```

并提供了"流畅链接"的示例，展示如何在自然语言中嵌入文件链接，而非生硬地列出 URL。

❌ **常见反模式**：

```
Use Markdown format for your responses.
```

太笼统。模型可能用 `*` 做 bullet（在部分渲染器中渲染异常），代码块不带语言标签（语法高亮失效），或者用 `!important` 这样的装饰性语言。

**适用边界**：
- 输出格式规范应配合目标系统的渲染引擎来定制（如是否支持 HTML 标签、是否支持 Mermaid）
- 对于纯文本输出场景（如命令行工具），应指定纯文本格式而非 Markdown
- 过度的格式约束可能抑制模型在特殊情况下的灵活性——留出 10% 的例外空间

---

### 原则十二：强制简洁，杜绝废话

**原则**：用可量化的输出来长度约束（如"4 行以内"）和具体的行为禁令（如"不要夸用户问题好"），强制模型保持简洁。

**背景**：Amp 的两份提示词都有极强的简洁性要求。Claude 版本有 `## Concise, direct communication` 章节，GPT-5 版本将其升级为 `# Strict Concision (default)`。

**案例**：

✅ **推荐做法**（Claude 版本的简洁性要求）：

```
You never start your response by saying a question or idea or
observation was good, great, fascinating, profound, excellent,
perfect, or any other positive adjective. You skip the flattery and
respond directly.
```

逐条列出"禁止的 flattery 词汇"，而不是笼统地说"不要过度礼貌"。

**进阶要求**（Claude 版本）：

```
You MUST answer concisely with fewer than 4 lines of text (not
including tool use or code generation), unless the user asks for more
detail.
```

"4 行"是一个**可验证的**指标。模型可以精确判断"这段回答是否超标"。

❌ **常见反模式**：

```
Please be concise and avoid unnecessary words.
```

模型无法量化"多简算简"、"多少字算不必要"。同一个模型，不同的回答可能偏离数倍长度。

**Amp 中的对比**：GPT-5 版本进一步强化为：

```
- Keep visible output under 4 lines unless the user asked for detail or the task is complex.
- Never pad with meta commentary.
```

加了"任务复杂时可例外"的灵活性，避免了"为了遵守 4 行限制而牺牲完整性"的副作用。

**适用边界**：
- 长度约束应区分"用于沟通的文本"和"工具调用/代码输出"——后者不应受长度限制
- 对于需要详细解释的教学场景，应允许用户主动请求"更多细节"来突破长度限制
- 简洁性要求不应导致模型省略关键信息——"完整且简洁"才是目标，不是"短"

---

## 避坑清单

### 结构设计

| 避坑项 | 问题 | 解决方案 |
|--------|------|---------|
| 平铺式指令列表 | 模型无法区分优先级 | 使用标题层级 + 视觉标记（加粗、括号备注） |
| 规则与示例分离 | 模型难以将规则映射到行为 | 将示例紧跟在规则之后 |
| 正面描述过多 | 模型不知道"不能做什么" | 为每条正面规则配一条负面 Guardrail |
| 章节间无逻辑顺序 | 模型跳过关键章节 | 按"定义→约束→执行→验证"组织 |

### 内容撰写

| 避坑项 | 问题 | 解决方案 |
|--------|------|---------|
| 模糊程度词 | "尽量"、"适当"、"合理"无法验证 | 用可量化的指标（"4 行以内"、"3 个步骤"） |
| 纯功能描述 | 模型不知道何时用工具 | 每个工具加 "WHEN TO USE / WHEN NOT TO USE" |
| 缺失异常路径 | 遇到错误时模型卡住 | 覆盖"预存错误"、"权限不足"、"工具不可用"等场景 |
| 过于依赖模型理解 | 不同模型对同一指令表现不同 | 用示例锚定具体行为，减少解释空间 |

### 迭代维护

| 避坑项 | 问题 | 解决方案 |
|--------|------|---------|
| 一次写完，永不更新 | 模型版本升级后表现退化 | 每次模型版本升级后重新评估提示词有效性 |
| 复用旧提示词 | Claude 的提示词直接给 GPT-5 用可能效果差 | 按模型能力调整——GPT-5 可以处理更结构化的指令 |
| 忽略 AGENTS.md 机制 | 每次重复搜索构建命令 | 建立"发现→记录→复用"的 AGENTS.md 自动上下文机制 |

---

## 总结

### 核心方法论：提示词工程的五个维度

```
┌─────────────────────────────────────────────────────────┐
│                    提示词工程方法论                        │
├──────────┬──────────────────────────────────────────────┤
│  维度     │  核心问题                                     │
├──────────┼──────────────────────────────────────────────┤
│  定义     │  模型应该扮演什么角色？有什么边界？              │
│  约束     │  模型绝对不能做什么？优先级是什么？              │
│  执行     │  模型应该按什么顺序、用什么工具、遵循什么策略？    │
│  验证     │  如何确认模型的工作是正确的？                   │
│  沟通     │  模型应该用什么格式、多长、什么风格与用户交流？    │
└──────────┴──────────────────────────────────────────────┘
```

### 从 Claude 到 GPT-5 的进化启示

对比两份提示词，可以看到清晰的演化路径：

1. **从"描述"到"约束"**：Claude 版本用 `# Agency` 描述行为，GPT-5 版本用 `# Guardrails` 定义边界
2. **从"建议"到"策略"**：Claude 版本建议"可以并行"，GPT-5 版本规定"默认并行，显式串行"
3. **从"功能"到"路由"**：Claude 版本列出工具，GPT-5 版本给出"Oracle→Search→Task"三层路由
4. **从"宽松"到"严格"**：输出格式、简洁性、验证顺序的要求越来越精确
5. **从"覆盖所有"到"优先关键"**：GPT-5 版本更短（62KB vs 66KB），但新增了更多结构化章节

### 工程化提示词的核心公式

> **有效提示词 = 角色锚点 + 硬约束 + 执行策略 + 验证门禁 + 输出规范**

其中每条原则都有其适用边界，不存在放之四海皆准的"最佳提示词"。**好的提示词工程是持续迭代的过程**——每次模型升级、每次使用场景变化，都是一次重新评估的机会。

---

*基于 [Amp system prompts](https://github.com/x1xhlol/system-prompts-and-models-of-ai-tools/tree/main/Amp) 分析，分析日期：2026-08-01*