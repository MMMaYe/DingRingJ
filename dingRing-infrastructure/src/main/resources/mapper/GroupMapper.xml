<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 群表 Mapper：成员列表 / 知识库配置 / 扩展字段均为 JSON -->
<mapper namespace="com.dingring.infrastructure.persistence.mapper.GroupMapper">

    <!-- ResultMap：3 个 JSON 字段各自指定 typeHandler -->
    <resultMap id="groupMap" type="com.dingring.domain.group.Group">
        <id column="id" property="id"/>
        <result column="group_member" property="groupMember"
                typeHandler="com.dingring.infrastructure.persistence.typehandler.GroupMemberListTypeHandler"/>
        <result column="knowledge_base_config" property="knowledgeBaseConfig"
                typeHandler="com.dingring.infrastructure.persistence.typehandler.JsonMapTypeHandler"/>
        <result column="feature" property="feature"
                typeHandler="com.dingring.infrastructure.persistence.typehandler.JsonMapTypeHandler"/>
    </resultMap>

    <sql id="columns">
        id, name, owner_id, group_member, knowledge_base_config, feature, deleted, create_time, update_time
    </sql>

    <select id="findById" resultMap="groupMap">
        SELECT <include refid="columns"/>
        FROM chat_group
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="findAll" resultMap="groupMap">
        SELECT <include refid="columns"/>
        FROM chat_group
        WHERE deleted = 0
        ORDER BY update_time DESC
    </select>

    <insert id="insert" parameterType="com.dingring.domain.group.Group"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_group (name, owner_id, group_member, knowledge_base_config, feature, create_time, update_time)
        VALUES (#{name}, #{ownerId},
                #{groupMember, typeHandler=com.dingring.infrastructure.persistence.typehandler.GroupMemberListTypeHandler},
                #{knowledgeBaseConfig, typeHandler=com.dingring.infrastructure.persistence.typehandler.JsonMapTypeHandler},
                #{feature, typeHandler=com.dingring.infrastructure.persistence.typehandler.JsonMapTypeHandler},
                #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.dingring.domain.group.Group">
        UPDATE chat_group
        SET name                  = #{name},
            owner_id              = #{ownerId},
            group_member          = #{groupMember, typeHandler=com.dingring.infrastructure.persistence.typehandler.GroupMemberListTypeHandler},
            knowledge_base_config = #{knowledgeBaseConfig, typeHandler=com.dingring.infrastructure.persistence.typehandler.JsonMapTypeHandler},
            feature               = #{feature, typeHandler=com.dingring.infrastructure.persistence.typehandler.JsonMapTypeHandler},
            update_time           = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 逻辑删除：保留历史消息/主题/卡片数据 -->
    <update id="deleteById">
        UPDATE chat_group SET deleted = 1, update_time = NOW() WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
